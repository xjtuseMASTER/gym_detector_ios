name: gym_detector_ios
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_timer_countdown: ^1.0.5 # 倒计时器 
  image_picker: ^0.8.5+3 # 照片采取器
  dotted_border: ^2.0.0
  video_thumbnail: ^0.5.1 # 视频压缩图
  shared_preferences: ^2.3.2 # 暂存用户偏好数据
  fl_chart:   #绘制曲线图
  http:  # http请求


  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/gymplan_images/
    - assets/user_images/
    - assets/dynamic_images/
    - assets/bar_images/
    - assets/bodydata_images/


  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf